<%= form_for(@text_map) do |f| %>
  <% if @text_map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_map.errors.count, "error") %> prohibited this text_map from being saved:</h2>

      <ul>
      <% @text_map.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
